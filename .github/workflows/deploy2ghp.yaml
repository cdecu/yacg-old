name: Deploy to GitHub Pages
on: [push]
jobs:
  Deploy-Ng-Yacg:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v2.1.5
        with:
          node-version: '14'
      - uses: actions/checkout@v2
        with:
          clean: false
      - name: Install
        run: yarn --version install
      - name: Lint
        run: node_modules/.bin/nx run ng-yacg:lint
      - name: Build
        run: DEBUG=* node_modules/.bin/nx run ng-yacg:build:production --progress --verbose --outputPath ${{ github.workspace }}/dist/ --baseHref=/yacg/
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.3
        with:
          branch: gh-pages
          folder: dist/
      - run: echo "🍏 This job's status is ${{ job.status }}."
